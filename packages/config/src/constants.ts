/** Local workbench directory name, relative to the project root. */
export const WORKBENCH_DIR = '.zarb';

/** Default config file path, relative to the project root. */
export const WORKBENCH_CONFIG_FILE = `${WORKBENCH_DIR}/config.local.yaml`;
